// =============================================================
// ChelBracket26 — config.js
//
// HOW TO SET UP FIREBASE (free, ~5 minutes):
//   1. Go to console.firebase.google.com → "Add project"
//   2. Name it "ChelBracket26", disable Google Analytics → Create
//   3. Left sidebar → Build → Firestore Database → Create database
//      → "Start in test mode" → pick any location → Enable
//   4. Left sidebar → Project Overview (⌂ icon) → "</> Web"
//      → Register app (nickname: ChelBracket26, skip hosting) → Done
//   5. Go to Project settings (gear icon) → Your apps → SDK setup
//      → Copy the apiKey and projectId values into the fields below
//
// Firebase API keys are SAFE to commit publicly.
// Security is handled by Firestore rules, not the key itself.
// =============================================================

window.CHELB_CONFIG = {
  apiKey:    'AIzaSyCAyag2E7GBAZMVCsaRBDrUlbC4s00QR5c',
  projectId: 'chelbracket26',
};
